# FedGNN_advanced — Tutorial & How-to

This tutorial shows how to build, run unit tests, and run benchmarks.

## 1) Build Python + Rust extension (optional)
Install maturin:
```bash
pip install maturin
